package Ejercicio18;

class Solution {
    public int strStr(String haystack, String needle) {
        // Use the built-in indexOf method to find the first occurrence of the needle
        return haystack.indexOf(needle);
    }
}